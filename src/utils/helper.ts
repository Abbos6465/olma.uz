export const numFormat = (value: number, format: string = ' '): string | number => {    if(!value) return 0;    return value.toString().replace(/\B(?=(\d{3})+(?!\d))/g, `${format}`);}export const generateRandomID = ():string => {    const characters:'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789' = 'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';    const charactersLength = characters.length;    let result = '';    for (let i = 0; i < charactersLength; i++) {        result += characters.charAt(Math.floor(Math.random() * charactersLength));    }    return result;}export const passingQueryNumber = (stringQuery: string): number | null => {    if(stringQuery && /^\d+$/.test(stringQuery)) return +stringQuery;    return null}export const findIndex = (items: any[], track: string, findValue):number => {    return items.findIndex(item => item[track] === findValue);}